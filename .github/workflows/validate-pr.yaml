name: Validate Reviewers Pull Request

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

jobs:
  checklist:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: 🧪 Checking whether reviewers have checked their duties
        run: |
          if ! echo "${{ github.event.pull_request.body }}" | grep -q '\[x\] Verifiquei todos os pontos da revisão'; then
            echo "Os dois revisores devem marcar o checkbox."
            exit 1
          else
            echo "Ambos os revisores marcaram o checkbox."
          fi
